/* 11. Write a JavaScript program to find the sum of squares
of a numeric vector. */
var sumSquares = function (a, b) {
  const firstSquare = (a * a)
  const secondSquare = (b * b)
  console.log(firstSquare + secondSquare)
}
sumSquares(5, -2)
